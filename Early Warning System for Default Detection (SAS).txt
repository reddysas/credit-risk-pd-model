data early_warning;
    input id dpd missed_payments;
    datalines;
1 0 0
2 30 1
3 60 2
4 90 3
;
run;

data risk_flag;
    set early_warning;
    if dpd > 60 or missed_payments >= 2 then risk="High";
    else risk="Low";
run;

proc freq data=risk_flag;
    tables risk;
run;